Drop your exported Figma images here (e.g. image1.png, image2.png, image3.png...)
then update the matching `src="..."` paths in `src/index.html` from the
Unsplash URLs to `./assets/images/yourfile.png`.
